# Movik — dependencias del proyecto
# pip install -r requirements.txt

# Data pull & processing
httpx[http2]>=0.27          # cliente HTTP async (el corazón del scraper)
beautifulsoup4>=4.12        # parser HTML
lxml>=5.0                   # backend rápido para bs4
duckdb>=1.0                 # query SQL directo sobre CSV sin cargar en RAM
openpyxl>=3.1               # export Excel

# Scraper
asyncio-throttle>=1.0       # rate limiting elegante

# Utils
python-dotenv>=1.0          # variables de entorno (.env)
rich>=13.0                  # output bonito en consola (progress, logs)
flask>=3.0                  # backend API + login para web admin